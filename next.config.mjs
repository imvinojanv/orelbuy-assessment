/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "cdnstatic.orelbuy.lk",
            "images.punkapi.com"
        ]
    }
};

export default nextConfig;
